Let me do this in two steps. First, I'll set up the data, then compute the result.

```repl
echo "setting up data"
echo "step one complete"
```

Now let me compute and return the answer.

```repl
echo "computing result"
RETURN "multi-block-done"
```